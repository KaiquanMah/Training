# count # rows in each col
df.count()

# check whether origin col contains the value, give me a col of true/false values
df['origin']=='Asia'
# return me only the rows with the matched value==True
df[df['origin']=='Asia']
