# Artistes still alive
SELECT name
FROM people
WHERE deathdate IS NULL;

# no budget film
SELECT title
FROM films
WHERE budget IS NULL;

# count films without language associated with them
SELECT COUNT(*)
FROM films
WHERE language IS NULL;
