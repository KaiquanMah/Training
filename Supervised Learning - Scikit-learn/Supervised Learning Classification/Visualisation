# Countplot
# Bein plot statements with figure(), otherwise plots will be overlayed onto the same figure
plt.figure()

# x axis = 'education', y axis and hue = 'party', colour palette in Red and Blue
sns.countplot(x='education', hue='party', data=df, palette='RdBu')

# set tick locations and labels
plt.xticks([0,1], ['No', 'Yes'])

# show the plot
plt.show()
